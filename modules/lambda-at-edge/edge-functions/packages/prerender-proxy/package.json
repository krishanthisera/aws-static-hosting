{
  "name": "@edge-functions/prerender",
  "version": "1.0.0",
  "description": "Prerender Edge Function",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build && node esbuild.js",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^8.3.0",
    "esbuild-runner": "^2.2.1"
  }
}
